[package]
name = "leafdb-core"
description = "Core storage and SQL engine for leafdb, a lightweight browser-first RDB."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
