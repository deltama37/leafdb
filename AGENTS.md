# リポジトリ開発ガイド (AGENTS.md)

leafdb は、Rust で実装し WebAssembly にコンパイルする、ブラウザファーストなリレーショナルデータベースである。
エンジン本体は `crates/leafdb-core`、WASM バインディングは `crates/leafdb-wasm`、
デモとJS/TS API は `web/` にある。設計方針は `docs/adr/` の ADR に記録する。

## 開発フロー: 設計と実装でモデルを分ける

機能追加や大きめの変更は、**設計を Claude Opus 5.5、実装を Grok 4.7** が担当する。
親エージェントは Opus 5.5 で起動し、実装だけを Grok 4.7 のサブエージェントに任せる。
この指示自体が、サブエージェントのモデル指定に関するユーザーからの明示的な依頼である。

小さな修正（typo、1〜2 行の変更、ドキュメントのみの変更）では、このフローを省略してよい。

### 1. 設計フェーズ（親エージェント / Opus 5.5）

- 設計書を `docs/design/<機能名>.md` に作成する（ファイル名はケバブケース）。
- 方針レベルの決定（言語仕様・ツールチェーンの選定など）は、設計書とは別に `docs/adr/NNNN-<タイトル>.md` の ADR として残す。
- 設計書には次を含める。
  - 目的と、対象外にすること
  - 関連する ADR
  - 変更・追加するパッケージとファイル
  - 公開する型・関数のシグネチャ（Rust のコード）
  - 処理の流れ（疑似コード）
  - エラーケースと、その扱い方
  - テスト観点（テーブル駆動テストのケース一覧）
  - 完了条件
- 実装者が迷わず実装できる粒度まで具体化する。判断を実装者に委ねない。

### 2. 実装フェーズ（サブエージェント / Grok 4.7）

- `Task` サブエージェントを `model: grok-4.7-high` で起動する。
- プロンプトには、設計書のパス、対象ブランチ、完了条件（`make check` が通ること）を含める。
- サブエージェントには次を守らせる。
  - 設計書に書かれた範囲だけを実装する。設計と食い違う点や不明点があれば、推測で進めずに報告する。
  - 設計書を書き換えない。
- 実装が大きい場合は、設計書の単位ごとにサブエージェントを分けて順番に実行する。

### 3. レビューフェーズ（親エージェント / Opus 5.5）

- 設計書と実装の差分を突き合わせ、設計から外れた点、テストの不足、エラー処理の漏れを確認する。
- 問題があれば、指摘内容を添えて実装サブエージェントに修正を依頼する。設計の誤りだった場合は、先に設計書を直す。
- `make check` が通ることを自分でも確認してから、コミット・PR 作成に進む。

## ビルドとテスト

- `make check` … フォーマット確認・lint・テスト・WASM ビルドをまとめて実行する（CI 相当の一括チェック）。
- 個別に実行する場合は次を使う。
  - `cargo fmt --all -- --check` … フォーマット確認
  - `cargo clippy --workspace --all-targets -- -D warnings` … lint（警告をエラー扱い）
  - `cargo test --workspace` … ネイティブのエンジンテスト
  - `./scripts/build-wasm.sh` … `web/pkg` への WebAssembly ビルド
- ローカルデモは `python3 -m http.server 8080 --directory web` で配信し、`http://localhost:8080/` を開く
  （Cloud Agent 環境では `.cursor/environment.json` の `terminals` が自動で起動する）。

## 環境

- Cloud Agent 環境はリポジトリ管理（`.cursor/environment.json`）。
  `install` は `./.cursor/install.sh`（Rust ツールチェーン・`wasm32-unknown-unknown` ターゲット・`wasm-pack` を用意し、
  ネイティブと WASM をビルドする）。`terminals` の `web` がデモを配信する。
